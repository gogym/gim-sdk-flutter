name: gim_im_sdk
description: GIM IM SDK - Flutter 即时通讯核心能力 SDK，基于 TCP + Protobuf
version: 0.1.0
homepage: https://github.com/gogym/gim_im_sdk

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  protobuf: ^3.1.0
  fixnum: ^1.1.0
  flutter_webrtc: '>=0.12.0 <2.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
